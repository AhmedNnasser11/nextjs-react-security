[build-system]
requires = []
build-backend = "setuptools.build_meta"

[project]
name = "nextjs-react-security"
version = "1.0.0"
description = "Production-oriented Next.js/React security Skill support engine"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
