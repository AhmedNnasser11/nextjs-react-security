# Dependency Security

Determine the package manager and run the actual audit command. Correlate exact resolved versions against current advisory sources. Distinguish vulnerable dependency existence from reachable vulnerable code.

Never fabricate audit results and never blindly upgrade all packages.
