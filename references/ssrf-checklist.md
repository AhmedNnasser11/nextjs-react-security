# SSRF

Inspect server-side requests influenced by user input: `fetch`, image proxies, URL previews, import-from-URL, webhooks, PDF/image fetchers and metadata requests.

Analyze localhost, loopback, private ranges, cloud metadata, redirects, DNS rebinding, alternate IP forms and protocol confusion. Validate the destination rather than only the URL string.
