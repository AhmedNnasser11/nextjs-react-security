# CSRF

Determine actual exploitability. Analyze cookie authentication, SameSite, Origin checks, state-changing requests, Server Actions, CORS, credentialed cross-origin requests, CSRF tokens and framework protections.

Do not add redundant infrastructure without identifying a real gap.
