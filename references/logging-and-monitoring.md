# Logging and Monitoring

Review security-relevant events: login success/failure, password changes, MFA, role/permission changes, admin actions and security-sensitive mutations.

Never log passwords, raw session tokens, secrets, API credentials or full authentication material.
