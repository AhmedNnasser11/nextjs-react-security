# File Upload Security

Review authorization, size, extension, MIME, magic bytes/content, filename/path handling, storage, public exposure, SVG/HTML risks, archives, executability, signed URLs, tenant isolation, malware scanning and metadata.

Extension checks alone are insufficient.
