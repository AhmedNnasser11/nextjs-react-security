# Secrets and Environment

Review `.env*`, `NEXT_PUBLIC_*`, server-only variables, build output, client bundles, logs, error responses, source maps, API keys, DB credentials, webhook secrets, OAuth secrets and cloud credentials.

Never expose server secrets in client bundles. Do not print secret values in reports.
